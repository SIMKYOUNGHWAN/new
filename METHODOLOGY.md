# 방법론

이 문서는 대시보드의 각 그래프가 **어떤 가정 위에서** 계산되는지 밝힙니다.
전문가는 그래프 개수보다 이 문서를 먼저 봅니다. 가정을 숨기면 신뢰를 잃습니다.

---

## 0. 먼저 알아야 할 것

환율 단기 예측에서 정교한 모델이 랜덤워크(오늘 값이 내일 값)를 이기지 못한다는 것은
1983년 Meese-Rogoff 연구 이후 반복 확인된 결과입니다. 이 대시보드는 그 사실을
숨기지 않습니다. 백테스팅에 랜덤워크를 벤치마크로 넣어두었고, 실제로 ARIMA가
그보다 나쁜 결과를 낼 때도 그대로 표시합니다.

따라서 이 대시보드의 목적은 **"환율을 맞추는 것"이 아니라 "얼마나 모르는지를
정직하게 보여주는 것"** 입니다.

---

## 1. 데이터

| 항목 | 출처 | 주기 | 비고 |
|---|---|---|---|
| USD/KRW | 한국은행 ECOS (731Y001) | 일별 | 영업일만 |
| 한국 기준금리 | ECOS (722Y001) | 월별 | |
| 미국 정책금리 | FRED (FEDFUNDS) | 월별 | |
| 달러인덱스 | FRED (DTWEXBGS) | 일별 | |
| 유가 | FRED (DCOILWTICO) | 일별 | |
| 경상수지·자본유출입·CDS·내재변동성·감성지수 | **미연동 (샘플)** | — | 통계표 코드 매핑 필요 |

**갱신**: GitHub Actions 가 평일 09:00 KST 에 배치를 돌려 `snapshot.json` 을 커밋합니다.
주말·공휴일은 시장 데이터가 없어 건너뜁니다.

**한계**
- 위 표의 하위 5개 지표는 아직 실데이터가 아닙니다. 해당 그래프는 형태 참고용입니다.
- 거시 지표는 월별이라 일별 환율과 주기가 다릅니다. 정렬 시 전진 채움(ffill)을
  쓰므로, 발표 시차만큼 실제보다 늦게 반영됩니다.
- 발표 시차가 있는 지표(경상수지 등)는 예측 시점에 아직 알 수 없었던 값이
  섞일 수 있습니다. 엄밀한 백테스팅을 하려면 vintage 데이터가 필요합니다.

---

## 2. 시나리오 밴드 (P-02)

**계산**: 과거 월간 로그수익률의 표준편차 σ 를 구하고, t 개월 뒤 확산 폭을
σ·√t 로 벌립니다. 중립은 과거 평균 드리프트를 따라가고, 강세·약세는 거기서
확산 폭만큼 뺀·더한 경로입니다.

**가정**
- 수익률이 정규분포를 따른다
- 과거 변동성이 미래에도 유지된다
- 평균 드리프트가 지속된다

**한계**: 위 세 가정은 모두 현실에서 자주 깨집니다. 특히 드리프트 외삽은
장기로 갈수록 위험합니다. 12개월 지점의 값을 목표가처럼 읽지 마세요.

---

## 3. ARIMA / 신뢰구간 (E-13)

**모델**: ARIMA(1,1,1), 80% 신뢰구간.

**가정**: 1차 차분 후 정상성, 선형 구조, 정규 오차.

**한계**
- 차수 (1,1,1) 을 고정했습니다. 자동 차수 선택(auto_arima)을 쓰지 않았습니다.
- 정상성 검정(ADF 등)을 명시적으로 수행하지 않습니다.
- 구조 변화(외환위기, 코로나 등)를 반영하지 못합니다.
- `statsmodels` 가 없거나 적합에 실패하면 랜덤워크로 폴백하며, 그 사실을
  화면에 `Random Walk (폴백)` 으로 표시합니다.

---

## 4. GARCH 변동성 (E-08)

**모델**: GARCH(1,1), t 분포 오차. 연율화하여 표시.

**가정**: 변동성 군집(큰 변동 뒤 큰 변동), 대칭적 충격 반응.

**한계**: 실제 환율은 하락 충격에 변동성이 더 커지는 비대칭성이 있는데,
GARCH(1,1)은 이를 잡지 못합니다. EGARCH/GJR-GARCH 가 필요합니다.
`arch` 미설치 시 EWMA 로 폴백합니다.

---

## 5. 몬테카를로 (E-09)

**계산**: 과거 월간 로그수익률의 평균 μ 와 표준편차 σ 를 추정하고,
기하 브라운 운동으로 5,000개 경로를 생성해 5·25·50·75·95 분위를 그립니다.

**가정**: 로그정규분포, μ·σ 가 미래에도 일정, 경로 간 독립.

**한계**
- 실제 환율 수익률은 정규분포보다 꼬리가 두껍습니다. 극단 상황의 확률을
  과소평가합니다.
- σ 를 과거 전 구간에서 추정하므로 최근 변동성 변화를 늦게 반영합니다.
- 난수 시드를 42로 고정해 재현 가능하게 했습니다.

---

## 6. VaR / CVaR (E-17)

**계산**: 정규분포 가정 하에 95% VaR = spot·(exp(1.645·σ√t) − 1),
99% CVaR = spot·(exp(2.665·σ√t) − 1). 원화 약세(환율 상승) 방향의 손실폭입니다.

**한계**: 정규분포 가정이라 실제 극단 손실을 과소평가합니다.
과거 분포 기반(historical VaR)이나 극단값이론(EVT)이 더 보수적입니다.

---

## 7. 요인 분해 · 특징중요도 (E-18, E-14)

**계산**: 월별 환율 변동분을 금리차·무역수지·CDS 변동분에 대해 최소제곱
회귀하고, 각 요인의 기여도(계수 × 변동분)를 스택으로 표시합니다.

특징중요도는 단위가 다른 요인을 비교하기 위해 **표준화 계수**
(β × 요인 표준편차 ÷ 종속변수 표준편차)의 절대값 비중으로 계산합니다.
원계수를 그대로 쓰면 단위 차이 때문에 비교가 무의미합니다.

**한계**
- **상관관계이지 인과관계가 아닙니다.**
- 설명변수 3개뿐이라 누락변수 편의가 큽니다.
- 다중공선성을 검정하지 않았습니다.
- 회귀계수를 전체 구간에서 고정 추정하므로 시기별 변화를 반영하지 못합니다.

---

## 8. 방향 예측 (P-05, E-21~E-24)

**목표**: 약 21영업일(1개월) 뒤 환율이 현재보다 높을 확률.

**특징 변수**: RSI(14), MA24 대비 비율, 4주 수익률, 12주 실현변동성,
단기/장기 모멘텀 비율, 52주 고점 대비, DXY 4주 변화, 한미 금리차, 내재변동성.
결측이 40%를 넘는 변수는 자동 제외합니다.

**모델**: 로지스틱 회귀(해석용)와 LightGBM(성능용)을 나란히 학습합니다.
둘 다 `class_weight="balanced"` 를 적용해 클래스 불균형을 보정합니다.

**검증**: walk-forward 방식만 사용합니다. 초기 250일로 학습 → 다음 21일 예측 →
학습 구간을 늘려 반복. 전체 데이터로 학습해 같은 데이터로 적중률을 재는
방식은 쓰지 않습니다.

### 적중률을 읽는 법 — 중요

단순 적중률만 보면 반드시 착시가 생깁니다. 화면에는 세 숫자를 함께 표시합니다.

| 지표 | 의미 |
|---|---|
| **적중률** | 맞힌 비율 |
| **기준선** | 다수 클래스만 계속 찍었을 때의 적중률 |
| **균형 정확도** | 상승 재현율과 하락 재현율의 평균 |

예를 들어 실제 상승이 34%인 구간에서는 "무조건 하락"만 찍어도 적중률 66%가
나옵니다. 이때 적중률 63%인 모델은 **기준선보다 못한** 것입니다.

**우위(edge)** 항목이 음수면 그 모델은 쓸모가 없다는 뜻이고, 대시보드는
이를 숨기지 않고 그대로 표시합니다.

**한계**
- 대표 모델은 균형 정확도가 높은 쪽을 자동 선택합니다. 검증 구간에서
  우연히 좋았던 모델이 뽑힐 수 있습니다.
- 표본이 수백 건 수준이라 통계적 유의성이 낮습니다.
- 거래비용·슬리피지를 고려하지 않았습니다. 적중률 55%로는 실제 매매에서
  수익이 나지 않을 가능성이 높습니다.

---

## 9. 백테스팅 (E-10)

**방식**: 마지막 12개월을 검증 구간으로 떼어내고, 이전 구간으로 학습한 뒤
RMSE 를 비교합니다. 랜덤워크·이동평균·ARIMA·드리프트·앙상블 5종.

**한계**: 단일 분할이라 구간 선택에 따라 순위가 바뀝니다. 엄밀하게는
여러 시작점에서 반복하는 rolling-origin 검증이 필요합니다.

---

## 10. 예측 이력 추적 (E-25)

매 배치마다 그날의 상승확률과 1개월 수치 전망을 `predictions.json` 에 기록하고,
30일이 지나면 실제값과 대조해 채점합니다.

**읽는 법**: 90% 밴드를 제시했다면 실제값이 밴드 안에 들어온 비율이 90% 근처여야
정상입니다. 이 비율이 크게 낮으면 모델이 불확실성을 과소평가하는 것이고,
크게 높으면 밴드가 지나치게 넓은 것입니다.

**한계**: 현재는 채점 시 "30일 경과 시점의 최신 환율"을 실제값으로 씁니다.
정확히 예측 시점 +30일의 값을 쓰도록 개선할 여지가 있습니다.

---

## 11. 이 대시보드로 하지 말아야 할 것

- 특정 숫자를 목표가로 삼아 매매하기
- 적중률을 기준선과 비교하지 않고 읽기
- 요인분해 결과를 인과관계로 해석하기
- 샘플 데이터 구간(하위 5개 지표)을 실제 지표로 오인하기

**본 자료는 투자 자문이 아닙니다.**

---

## 변경 이력

| 날짜 | 내용 |
|---|---|
| 초기 | 시나리오·ARIMA·GARCH·몬테카를로·VaR·요인분해·백테스팅 |
| 갱신 | 방향 예측(로지스틱·LightGBM), 예측 이력 추적, 방법론 문서 추가 |
