# 방법론 (조지아 라리)

이 문서는 대시보드의 각 그래프가 **어떤 가정 위에서** 계산되는지 밝힙니다.
전문가는 그래프 개수보다 이 문서를 먼저 봅니다. 가정을 숨기면 신뢰를 잃습니다.

---

## 0. 먼저 알아야 할 것

환율 단기 예측에서 정교한 모델이 랜덤워크(오늘 값이 내일 값)를 이기지 못한다는 것은
1983년 Meese-Rogoff 연구 이후 반복 확인된 결과입니다. 이 대시보드는 그 사실을
숨기지 않습니다. 백테스팅에 랜덤워크를 벤치마크로 넣어두었고, 다른 모델이
그보다 나쁜 결과를 낼 때도 그대로 표시합니다.

**라리는 원화보다 예측이 더 어렵습니다.** 아래 두 가지 때문입니다.

1. **NBG 개입** — 조지아 국립은행이 외환시장에 자주 개입합니다. 개입 중에는
   환율이 며칠씩 사실상 고정되는데, 이 구간은 상승도 하락도 아니면서
   이진 분류에서는 한쪽으로 강제 배정됩니다.
2. **관광 계절성** — 여름 성수기에 외화가 몰려 라리가 강세를 보이는 패턴이
   뚜렷합니다. 계절성을 빼지 않으면 추세 변화로 오해하게 됩니다.

---

## 1. 데이터

| 항목 | 출처 | 주기 | 비고 |
|---|---|---|---|
| USD/GEL, EUR/GEL, RUB/GEL, TRY/GEL, AMD/GEL, AZN/GEL | NBG API | 영업일 | 아래 참고 |
| 달러인덱스 | FRED (DTWEXBGS) | 일별 | |
| 유가(WTI) | FRED (DCOILWTICO) | 일별 | |
| VIX | FRED (VIXCLS) | 일별 | |
| 송금·관광·FDI·무역수지 | **미연동 (샘플)** | 월별 | 공개 API 미확인 |
| NBG 정책금리·외환보유고·라리화 비율·개입 | **미연동 (샘플)** | 월별 | 공개 API 미확인 |

### NBG API 의 제약

엔드포인트: `nbg.gov.ge/gw/api/ct/monetarypolicy/currencies/en/json/?currencies=USD&date=YYYY-MM-DD`

이 API 는 **날짜를 하나씩만** 조회할 수 있습니다. 3년치를 매번 받으면 750회
호출이 되어 배치가 느려지고 차단당합니다. 그래서 받은 값을 `data/nbg_rates.csv`
에 캐시해 두고 **빠진 날짜만 증분으로** 채웁니다. 한 번의 배치에서 최대
120일까지만 받으므로, 과거분은 여러 번의 배치에 걸쳐 서서히 채워집니다.
그동안 화면에 남은 일수가 표시됩니다.

### quantity 정규화 — 놓치면 100배 오차

NBG 응답의 `rate` 는 `quantity` 단위당 가격입니다. 통화마다 값이 다릅니다.

| 통화 | rate | quantity | 실제 1단위 가격 |
|---|---|---|---|
| USD | 2.6741 | 1 | 2.6741 |
| RUB | 3.05 | 100 | 0.0305 |
| AMD | 7.2691 | 1000 | 0.0072691 |

`rate / quantity` 로 나누지 않으면 루블은 100배, 드람은 1000배 틀어집니다.

### 갱신 시각

NBG 는 트빌리시 시각 17시경에 공시하고, 기준일은 화~토입니다.
배치는 UTC 14:00(트빌리시 18:00) 평일에 실행합니다.

### 조지아 거시지표에 대하여

NBG 와 Geostat 이 송금·관광·FDI·무역수지·외환개입을 매월 공표하는 것은
확인했으나, **기계가 읽을 수 있는 공개 API 는 확인하지 못했습니다.**
현재 해당 그래프는 샘플이며, 화면 상단에 어떤 지표가 샘플인지 경고로
표시됩니다. 엔드포인트를 찾으면 `sources.py` 의 해당 블록만 교체하면 됩니다.

**개별 지표 실패 시 동작**: 환율(NBG)이 실패하면 배치를 중단하고 이전 스냅샷을
유지합니다. 보조 지표는 개별적으로 실패해도 샘플로 대체하고 진행합니다.

---

## 2. 시나리오 밴드 (P-02)

**계산**: 과거 월간 로그수익률의 표준편차 σ 를 구하고, t 개월 뒤 확산 폭을
σ·√t 로 벌립니다. 중립은 과거 평균 드리프트를 따라가고, 강세·약세는 거기서
확산 폭만큼 뺀·더한 경로입니다.

**가정**: 수익률 정규분포, 과거 변동성 유지, 드리프트 지속.

**한계**: 세 가정 모두 현실에서 자주 깨집니다. 특히 라리는 NBG 개입으로
변동성이 인위적으로 억제되는 구간이 있어, 과거 σ 가 미래 위험을
과소평가할 수 있습니다.

---

## 3. 계절성 분해 (E-13, E-14)

**모델**: STL (Seasonal-Trend decomposition using Loess), 주기 12개월, robust 옵션.

**읽는 법**: 월별 계절 효과가 양수면 그 달에 환율이 오르는(라리 약세) 경향,
음수면 내리는(라리 강세) 경향입니다. 여름 관광 성수기에 음수가 나오는 것이
전형적인 패턴입니다.

**계절성 강도**는 0~1 사이 값으로, 계절 성분의 분산을 계절+잔차 분산으로
나눈 것입니다. 높을수록 매년 같은 시기에 반복되는 패턴이 뚜렷합니다.

**한계**: 30개월 이상 데이터가 필요합니다. 2022년 우크라이나 전쟁처럼
구조가 바뀌는 사건이 있으면 그 전후를 하나의 계절 패턴으로 묶어버립니다.

---

## 4. SARIMA (E-16)

**모델**: SARIMA(1,1,1)(1,0,1)[12]. 계절항 실패 시 일반 ARIMA(1,1,1),
그마저 실패하면 랜덤워크로 폴백하며 화면에 어느 것을 썼는지 표시합니다.

**한국판과의 차이**: 원화는 계절성이 약해 ARIMA 로 충분했지만, 라리는
관광 계절성 때문에 계절항이 필요합니다.

**한계**: 차수를 고정했습니다. 자동 차수 선택을 쓰지 않았고 정상성 검정도
명시적으로 하지 않습니다.

---

## 5. GARCH · 몬테카를로 · VaR

원화 버전과 동일한 방식입니다. GARCH(1,1)-t 로 변동성을,
기하 브라운 운동 5,000경로로 확률 밴드를, 정규분포 가정으로 VaR/CVaR 을 냅니다.

**라리 특유의 한계**: NBG 개입으로 변동성이 눌린 구간이 섞여 있어,
추정된 σ 가 실제 위험보다 낮게 나올 수 있습니다. 개입이 멈추면
실제 변동은 이 밴드를 크게 벗어날 수 있습니다.

---

## 6. 요인 분해 (E-21)

**요인**: 송금·관광·투자(FDI)·정책(NBG 금리). 조지아는 이 넷이 외화 공급의
대부분을 차지합니다. 한국판의 금리차·무역수지·CDS 는 쓰지 않습니다.

**계산**: 월별 환율 변동분을 각 요인 변동분에 최소제곱 회귀하고, 기여도를
스택으로 표시합니다. 특징중요도는 단위 차이를 없애기 위해 표준화 계수 기준
상대 비중으로 계산합니다.

**한계**: **상관관계이지 인과관계가 아닙니다.** 현재 요인 데이터가 샘플이므로
이 그래프의 수치는 참고용조차 되지 못합니다. 실데이터 연동 후에만 의미가 있습니다.

---

## 7. 방향 예측 (P-05, E-22~E-26)

**목표**: 약 21영업일(1개월) 뒤 환율이 현재보다 높을 확률.

**특징 변수**: RSI(14), MA24 대비 비율, 4주 수익률, 12주 실현변동성,
단기/장기 모멘텀, 52주 고점 대비, DXY·유가 4주 변화, VIX 수준,
EUR/RUB/TRY 대비 라리 4주 변화, **계절성 항(sin/cos)**, **무변동 지속일**.

마지막 두 가지가 원화 버전과의 차이입니다. 계절성은 관광 패턴을,
무변동 지속일은 NBG 개입 구간을 모델에 알려줍니다.

**모델**: 로지스틱 회귀(해석용)와 LightGBM(성능용)을 나란히 학습하고,
균형 정확도가 높은 쪽을 대표로 씁니다.

**검증**: walk-forward 만 사용합니다. 초기 250일 학습 → 다음 21일 예측 →
학습 구간을 늘려 반복.

### 적중률을 읽는 법

| 지표 | 의미 |
|---|---|
| **적중률** | 맞힌 비율 |
| **기준선** | 다수 클래스만 계속 찍었을 때의 적중률 |
| **균형 정확도** | 상승 재현율과 하락 재현율의 평균 |
| **무변동 비중** | 1개월 변화율이 0.1% 미만인 구간의 비율 |

**무변동 비중이 라리에서 특히 중요합니다.** NBG 개입으로 환율이 고정된
구간은 상승도 하락도 아닌데 이진 분류에서 한쪽으로 강제 배정됩니다.
이 비중이 높을수록 적중률의 의미가 약해집니다.

**우위(edge)** 가 음수면 그 모델은 쓸모가 없다는 뜻이고, 그대로 표시합니다.

### 확률 보정 (E-26)

LightGBM 은 확률을 0/1 쪽으로 과하게 밀어냅니다. walk-forward 로 얻은
out-of-fold 확률의 로짓에 로지스틱을 다시 적합하는 **Platt scaling** 으로
보정합니다. 학습에 쓰지 않은 예측만 사용하므로 정보 누수가 없습니다.

**중요**: 보정은 확률의 눈금만 조정하며 예측 순서를 바꾸지 않습니다.
따라서 **적중률과 균형 정확도는 보정 전후가 같습니다.** 모델이 더 잘
맞게 되는 것이 아니라, 확률 숫자를 정직하게 읽을 수 있게 되는 것입니다.

---

## 8. 백테스팅 (E-18)

마지막 12개월을 검증 구간으로 떼어내고 랜덤워크·이동평균·ARIMA·드리프트·
앙상블 5종의 RMSE 를 비교합니다. 단일 분할이라 구간 선택에 따라 순위가 바뀝니다.

---

## 9. 예측 이력 추적 (E-27)

매 배치마다 상승확률과 1개월 수치 전망을 기록하고, 30일이 지나면 실제값과
대조해 채점합니다. 90% 밴드를 제시했다면 실제값이 밴드 안에 들어온 비율이
90% 근처여야 정상입니다.

---

## 10. 참고 뉴스

배치에서 하루 한 번 조지아 매체 RSS 를 읽어 경제·환율 키워드가 제목에 있는
기사만 추립니다.

**저작권**: 기사 본문은 저장하지도 표시하지도 않습니다. 제목·출처·발행일·
링크만 보관하고 본문은 원문 링크로 보냅니다.

**한계**: 뉴스와 환율 사이에 **인과관계가 있다는 뜻이 아닙니다.** 제목에
키워드가 있다는 이유만으로 모은 것입니다. 차트 옆이 아니라 페이지 하단에
따로 배치한 것도 이 때문입니다.

**운영 참고**: 매체가 RSS 주소를 바꾸거나 봇 접근을 막는 일이 잦습니다.
수집이 계속 실패하면 `app/news.py` 의 `FEEDS` 를 갱신하세요.

---

## 11. 이 대시보드로 하지 말아야 할 것

- 특정 숫자를 목표가로 삼아 매매하기
- 적중률을 기준선·무변동 비중과 함께 읽지 않기
- 요인분해 결과를 인과관계로 해석하기 (현재는 샘플 데이터이므로 더욱)
- 샘플 지표(송금·관광·FDI·라리화 비율 등)를 실제 수치로 오인하기
- 참고 뉴스를 환율 움직임의 원인으로 단정하기

**본 자료는 투자 자문이 아닙니다.**

